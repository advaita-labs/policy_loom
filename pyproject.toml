[project]
name = "policy-loom"
version = "0.1.0"
description = "Open-source pipeline for ingesting robotics datasets and training pi0/diffusion/smolVLA policies with policy-aware preprocessing and a hexagonal repo design"
authors = [
    { name = "copilot-swe-agent[bot]", email = "198982749+Copilot@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.19,<0.9.0"]
build-backend = "uv_build"
